'use client';

import Link from 'next/link';
import styled from 'styled-components';

const Logo = () => {
  return (
    <div>Logo</div>
  );
};

const StyledLogo = styled(Link)`
  
`;

export default Logo;
