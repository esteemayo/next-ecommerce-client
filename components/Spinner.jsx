'use client';

import styled from 'styled-components';
import { ClipLoader } from 'react-spinners';

const Spinner = () => {
  return (
    <div>Spinner</div>
  );
};

export default Spinner;
