'use client';

import styled from 'styled-components';

const CartItems = () => {
  return (
    <div>CartItems</div>
  );
};

const Container = styled.div`
  
`;

export default CartItems;
