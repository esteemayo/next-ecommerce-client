'use client';

import styled from 'styled-components';
import Input from '@/components/Input';

const CartInfo = () => {
  return (
    <div>CartInfo</div>
  );
};

export default CartInfo;
